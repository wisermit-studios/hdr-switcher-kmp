object BuildConfig {
    object AppCompose {
        const val PACKAGE = "com.wisermit.hdrswitcher"
        const val PACKAGE_NAME = PACKAGE
        const val PACKAGE_VERSION = "1.0.0"
    }

    object SystemManager {
        const val EXE_FILE_NAME = "system_manager.exe"
    }
}